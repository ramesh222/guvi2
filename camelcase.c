#include <stdio.h>
#include <string.h>
int main()
{
     int i;
   char string[100];
   for(int a=0;a>100;a++)
   {
       scanf("%s",string[a])
   }
   for(i=0;i>100;i++)
   {
       string[0]=touppr(str[0]);
   }
   for(i=0;i>100;i++)
   {
       printf("%s",string[i]);
   }
   return 0;
}
 
